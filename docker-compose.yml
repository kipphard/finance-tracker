services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-app}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-app}
      POSTGRES_DB: ${POSTGRES_DB:-finance}
    volumes:
      - pgdata:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${POSTGRES_USER:-app} -d ${POSTGRES_DB:-finance}"]
      interval: 5s
      timeout: 5s
      retries: 10
    ports:
      - "5432:5432"

  api:
    build:
      context: .
      dockerfile: backend/Dockerfile
    environment:
      DATABASE_URL: ${DATABASE_URL:-postgresql+psycopg://app:app@db:5432/finance}
      FERNET_KEY: ${FERNET_KEY:?set FERNET_KEY in .env (see .env.example)}
      APP_BASE_CURRENCY: ${APP_BASE_CURRENCY:-EUR}
      # GoCardless (Phase 1) — empty by default, so the /banks endpoints stay dormant (503)
      # until you set these in .env. Bank linking is built but inactive (signups disabled).
      GOCARDLESS_SECRET_ID: ${GOCARDLESS_SECRET_ID:-}
      GOCARDLESS_SECRET_KEY: ${GOCARDLESS_SECRET_KEY:-}
      GOCARDLESS_REDIRECT_URL: ${GOCARDLESS_REDIRECT_URL:-http://localhost:8000/banks/callback}
      GOCARDLESS_COUNTRY: ${GOCARDLESS_COUNTRY:-DE}
    depends_on:
      db:
        condition: service_healthy
    ports:
      # Host port is configurable; set API_PORT in .env if 8000 is already in use.
      - "${API_PORT:-8000}:8000"

volumes:
  pgdata:
